query getPost($postId: Int) {
    findDetailDealByPost(postId: $postId) {
        dealId
        postId
        imgPaths{
            imgPath
        }
        title
        category
        hashtag
        contents
        price
        buyerId
        sellerId
        address
    }
}